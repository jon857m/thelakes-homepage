name = "thelakes-maintenance-gate"
main = "maintenance-worker.js"
compatibility_date = "2026-08-26"
workers_dev = false
preview_urls = false
routes = [
  { pattern = "www.thelakesincumbria.co.uk/*", zone_name = "thelakesincumbria.co.uk" },
  { pattern = "thelakesincumbria.co.uk/*", zone_name = "thelakesincumbria.co.uk" }
]

# Configure these with `wrangler secret put` rather than committing values:
# SUPABASE_URL
# SUPABASE_ANON_KEY
# BYPASS_SIGNING_SECRET
